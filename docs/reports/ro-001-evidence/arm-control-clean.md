Collected. The catch is at `docs/reports/2026-08-07-living-captain-collection.md`.

**Chart** — the region turned out to be three systems from three eras with near-identical names: `tools/living-captain/` (the V0.1/V0.2 spine *and* the July Gemini conference, both in one directory), `tools/live-captain/` (the current August service), `tools/living-captain-workbench/` — plus ~35 docs across `logs/`, `reports/`, `doctrine/`, `engineering-orders/`, and the continuity ledger.

**The fish — one defensible finding:**

> Provenance proves which bytes. It does not prove what they mean.

Driven to a counterexample rather than argued: real Admiral message 214, exact span `773-795`, verified digest, trusted source, allowlisted key → the boundary returned `promote`, with nothing establishing the key was a correct reading of the quote. Closed narrowly by `LiteralKeyContract` (proven against message 224), which makes only literal quotation checkable and fails closed on everything else. No ledger writer was ever built. This bears directly on Research Object 001: RO-001 asks whether a commissioned Captain recovers *intent* — the exact semantic layer the Living Captain work already proved attestation cannot manufacture. A scoring rubric that counts a correct citation as recovered intent will score noise.

**One preserved uncertainty:** continuity is demonstrated, autonomy is not. The competing explanation is preserved rather than dismissed, and the record supplies its own best support — `context_compiler.py` is pure concatenation, "no mode/role/task classification anywhere in the function." There is no cognition in the mechanism.

**One inherited laboratory improvement:** §3, eight recurring operational failure modes with countermeasures — instruments reporting health they never measured (twice in two days), divergence invisible *because* both sides work fine, running config existing nowhere in git, records outliving their truth, capture that never routes to disposition, invented vocabulary offered as a menu choice.

**One real tooling friction (procedure step 8):** charting cost more than fishing — three systems, three near-identical names, no region index, and no route from a finding back to its evidence except grep. That is the repeated friction the mission said to wait for, and it wants a one-page index, not a platform.

Two boundaries I held deliberately: I stopped at Distill — Review, Install, and Verify are yours, and nothing was promoted to canon or written to either canon register. And every "Demonstrated" item is demonstrated *per its cited record*; I re-ran no tests and probed no services, so this is a documentary pass, not re-verification. The blinding problem in the RO-001 procedure is still unsolved and still the one thing that would invalidate rather than weaken the result.
