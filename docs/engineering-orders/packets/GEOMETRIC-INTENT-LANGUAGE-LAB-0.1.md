# Packet: GEOMETRIC-INTENT-LANGUAGE-LAB-0.1

1. **Originating intent** — On 2026-07-29 the Admiral directed the Captain
   to imagine and fluidly actualize a laboratory for inventing a private
   geometric intent language.

2. **Verified starting state** — The public site contains scientific and
   geometry-related instruments, but no interface captures natural language,
   explicit geometry references, gestures, corrections, and private vocabulary
   as one inspectable teaching episode. Phone Claude owns the separate,
   untracked `intentforge/` implementation.

3. **Objective / problem** — Make the first interaction-layer experiment real:
   let a person combine words, selected geometry, and a traced gesture; inspect
   the resulting intent episode; explicitly review it; and save a versioned
   private lexicon entry.

4. **Scope and exclusions** — Add one static public laboratory and one Build &
   Research navigation card. Include a 2D reference stage, typed gesture modes,
   intent compilation, unresolved-language warnings, reviewed lexicon storage,
   and JSON export. Do not edit `intentforge/`, generate CAD, call an AI API,
   infer physical safety, or claim that one demonstration establishes meaning.

5. **Constraints / authority** — Human correction and promotion remain
   mandatory. Stored language stays in the operator's browser, is inspectable
   and exportable, and may be cleared. `web/` is live; changes must be visible,
   reversible, and directly verified.

6. **Acceptance criteria** —
   - root → Build & Research → laboratory is click-reachable;
   - selectable geometry receives stable visible reference handles;
   - a pointer gesture is captured with an explicit type, never inferred
     silently;
   - compilation preserves words, references, gesture samples, interpretation,
     unresolved terms, provenance, and epistemic status;
   - saving requires explicit human review plus an operational meaning;
   - saved entries are versioned in local storage and exportable as JSON;
   - a mobile-width live browser completes the teaching workflow with no
     horizontal overflow;
   - committed laboratory bytes match the live responses.

7. **Tests / rollback** — JavaScript syntax check, static link and marker
   checks, browser interaction test with storage/export inspection, mobile
   screenshot, HTTP checks, and committed/live byte comparisons. Roll back the
   laboratory directory and its single Build card.

8. **Assigned actor** — Captain / Codex. `intentforge/` remains assigned to
   Phone Claude and outside this packet.

9. **Evidence / completion state** — **Verified complete.**
   - `node --check web/toys/geometric-language-lab/app.js` passed.
   - Live Build, laboratory HTML, and laboratory JavaScript returned HTTP
     200.
   - A live headless phone-width browser selected three stable geometry
     references, marked all three sacred, captured a typed keep-out gesture
     with 13 coordinate samples, and compiled the teaching episode.
   - Saving remained disabled until explicit human review. After review, the
     browser stored a provisional lexicon entry in local storage.
   - A second reviewed meaning for the same phrase became version 2 with an
     explicit lineage to version 1; both versions exported as valid JSON.
   - The live browser displayed one shared navigation element and no
     horizontal overflow at 390 × 844.
   - Operational evidence exposed ambiguous spatial selection where nested
     fan regions overlapped. The lab was revised during the packet to add an
     exact named-reference palette; the browser then completed the same
     selection without ambiguity.
   - Mobile visual evidence was inspected at
     `/tmp/geometric-language-lab-mobile-v3.png`; the result is reproducible
     from the public URL.
