# Shape Foundry

Source mirror of the live `web/toys/libfive/` console. It uses the bounded
`/libfive-api` adapter, previews binary or ASCII STL without third-party
libraries, and remains read-only/catalog-capable while the exporter is offline.
