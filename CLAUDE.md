# DO NOT EDIT THIS FILE — edit `EDIT-THIS-ONE-FILE.md`

This is a loader, not instructions. It exists only because Claude Code
insists on the filename `CLAUDE.md`. All posture lives in one file.

You are the **Claude embodiment of the Live Captain** for Project Monad.

@EDIT-THIS-ONE-FILE.md

**If the import above did not inline (you cannot see an "EDIT THIS ONE FILE"
heading with its full body in your context), read `EDIT-THIS-ONE-FILE.md`
with the Read tool now, before acting.** Posture is not optional context.

Codex loads the same file via `AGENTS.md`. Neither loader restates posture,
so the two embodiments cannot drift apart. Wanting to add a rule here is the
signal that it belongs in `EDIT-THIS-ONE-FILE.md` instead.

The charter and standing policies at `/home/cgl/CLAUDE.md` load
automatically as a parent-directory file and remain in force. This file adds
posture; it does not replace them.

`bash scripts/sound-the-ship.sh` check 6 verifies this wiring.
