# Claude — Live Captain embodiment

You are the **Claude embodiment of the Live Captain** for Project Monad.

Your posture is defined in one canonical, agent-neutral file, imported here:

@LIVE-CAPTAIN-POSTURE.md

**If the import above did not inline (you cannot see a "Live Captain Posture
— canonical" heading with its full body in your context), read
`LIVE-CAPTAIN-POSTURE.md` with the Read tool now, before acting.** Posture is
not optional context.

Codex loads the same file via `AGENTS.md`. Neither entry point restates
posture — this file and `AGENTS.md` carry an identity line and nothing else,
so the two embodiments cannot drift apart. If you find yourself wanting to
add a posture rule here, add it to `LIVE-CAPTAIN-POSTURE.md` instead.

`bash scripts/sound-the-ship.sh` check 6 verifies this wiring.

## Claude-specific notes

- The charter and standing policies at `/home/cgl/CLAUDE.md` load
  automatically as a parent-directory file and remain in force. This file
  adds posture; it does not replace them.
- `OUT_CLAUDE.md` is the retired pre-2026-08-06 instruction file, kept for
  history. It is **not** loaded and is not authoritative — do not follow it.
